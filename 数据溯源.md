问题二是按照附件2的数据预测然后算的吗？

是的，问题二当前是**基于附件 2 的实际负荷和光伏数据构造预测，再用真实数据做当日结算**。但要区分两件事：

**1. 预测阶段只使用历史信息**

第 \(d\) 天 0:00 做计划时，只使用第 \(d-1\) 天及以前的数据：

\[ \{L_{d-1,t},V_{d-1,t},L_{d-2,t},V_{d-2,t},\dots\} \]

代码里的负荷和光伏点预测由 [problem2_stochastic.py (line 150)](D:/46884/Documents/2026/问题二/problem2_stochastic.py:150) 的 `build_point_forecasts()` 生成，它在递推过程中只读取此前的实际数据，不使用当天及未来实际值。

**2. 实际执行阶段使用当天附件 2 数据**

当天实际负荷和光伏到达后，才用于：

- 计算实际净负荷缺口；
- 决定储能实际充放电；
- 结算紧急购电量；
- 更新日末 SOC；
- 传递给下一天。

对应逻辑在 [problem2_adaptive.py (line 857)](D:/46884/Documents/2026/问题二/problem2_adaptive.py:857) 的逐日滚动循环中。

因此可以概括为：

\[ \text{计划阶段：历史附件 2 数据} \]\[ \text{执行阶段：当天附件 2 真实数据} \]

不是把附件 2 当天未来的数据提前给 0:00 计划模型。

附件 3 当前问题二主流程没有使用，附件 3 属于问题三的光伏预报数据。问题二的价格来自附件 1，负荷和光伏历史与实际执行数据来自附件 2，储能参数来自题目 PDF 附录 1。
- - -
你预测时有没有把附件 2 当天未来的数据提前给 0:00 计划模型？

没有。当前代码的 0:00 计划模型不会提前看到附件 2 当天未来的实际负荷和光伏数据。

具体分开看：

**0:00 计划阶段**

第 \(d\) 天计划只使用：

- 第 \(d-1\) 天及以前的实际负荷和光伏；
- 第 \(d-1\) 天及以前的预测误差；
- 当天已知的电价曲线；
- 从历史数据生成的情景。

不会使用：

- 当天未来的实际负荷；
- 当天未来的实际光伏；
- 当天实际净负荷缺口。

对应代码是 [problem2_stochastic.py (line 150)](D:/46884/Documents/2026/问题二/problem2_stochastic.py:150) 的点预测递推，以及历史误差情景生成逻辑。它虽然在内存中读取了附件 2 的完整 365 天数据，但 `build_point_forecasts()` 在第 \(d\) 天循环时只读取 `load_actual_kwh[:d]` 和 `pv_actual_kwh[:d]`，不会读取第 \(d\) 天实际值来构造第 \(d\) 天预测。

**实际执行阶段**

当天实际数据到达后，代码才使用：

\[ L_{d,t}^{\text{actual}},\quad V_{d,t}^{\text{actual}} \]

用于：

- 计算实际缺口；
- 执行储能放电；
- 结算紧急购电；
- 更新实际 SOC。

在时刻 \(t\) 做出充放电决策时，代码只使用：

- 当前时刻及以前的真实数据；
- 已经锁定的计划购电 \(g_t\)；
- 历史情景形成的未来价值函数。

`execute_day_with_future_value()` 不会读取当天 \(t+1\) 之后的真实值来决定当前动作。

所以准确表述是：

\[ \boxed{ \text{0:00 计划阶段不泄漏当天未来实际数据} } \]\[ \boxed{ \text{实际执行阶段按时间顺序使用当天已经发生的数据} } \]

当前主流程中附件 3 也没有参与问题二的计划模型。